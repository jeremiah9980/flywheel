[tool.ruff]
target-version = "py311"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = [
    "E501",  # line too long – long strings/docstrings are intentional
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
